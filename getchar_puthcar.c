#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*char myKey;
    printf("Push a button:\n");
    myKey= getchar();
    printf("You pushed %c button\n",myKey);
    printf("ASCII code is %d\n",myKey);
    */
    for(int i=65;i<=80;i++)
        {
        putchar(i);
        printf(" ");
        }

    return 0;
}
